<!-- src/LIGHTNING.App/UI/Views/Settings/SettingsView.xaml -->
<UserControl x:Class="LIGHTNING.App.UI.Views.Settings.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:tabs="clr-namespace:LIGHTNING.App.UI.Views.Settings.Tabs"
             mc:Ignorable="d">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0"
               Text="Settings"
               FontSize="20"
               FontWeight="SemiBold"
               Margin="0,0,0,12" />

    <TabControl Grid.Row="1"
                SelectedIndex="{Binding SelectedTabIndex}">
      <TabItem Header="Install Settings">
        <tabs:InstallSettingsView />
      </TabItem>

      <TabItem Header="Diagnostics">
        <tabs:DiagnosticsView />
      </TabItem>
    </TabControl>
  </Grid>
</UserControl>
